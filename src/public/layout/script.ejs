<% chunks.forEach(function(name,index){ %>
    <% if(typeof chuncksEntry[name] === 'object'){ %>
        <script type="text/javascript" src="<%=chuncksEntry[name].entry%>"></script>
    <% } %>
<% }) %>